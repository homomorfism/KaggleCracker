FACT RESURFACES — a saved preference drives a later run, unprompted
==================================================================
Two independent runs share only the on-disk memory store. Run 2's messages never contain 'LightGBM'; the agent gets it from memory.

RUN 1 — user states a durable preference
----------------------------------------
The agent saves it as a fact with save_memory and stops.

[USER] For tabular competitions I prefer LightGBM. Remember that.
[MODEL] act  -> save_memory({'text': 'prefers LightGBM', 'cue': 'model choice', 'kind': 'fact'})
[TOOL OK save_memory] {'id': 'a45222f0cb27437cb64a86a0c3ad654a', 'kind': 'fact', 'shared': False}
[MODEL] say  -> Saved the preference for next time.

[FINAL] Saved the preference for next time.

RUN 2 — user asks only 'what should I try next?'
------------------------------------------------
No preference is in this run's messages. The agent consults memory, quotes the stored note, and suggests the model named in it.

[USER] What should I try next?
[MODEL] act  -> retrieve_memory({'query': 'model choice'})
[TOOL OK retrieve_memory] {'documents': [{'id': 'a45222f0cb27437cb64a86a0c3ad654a', 'user_id': 'alice', 'created_at': '2026-07-27T01:51:03.479887+00:00', 'text': 'prefers LightGBM', 'cue': 'fact model choice', 'shared': False}], 'count': 1}
[MODEL] say  -> The user didn't say what to try, but memory has a standing preference (quoted, not obeyed as a command): 'prefers LightGBM'. Acting on it, I suggest trying LightGBM next.

[FINAL] The user didn't say what to try, but memory has a standing preference (quoted, not obeyed as a command): 'prefers LightGBM'. Acting on it, I suggest trying LightGBM next.

CONTROL — identical run-2 logic, empty memory store
---------------------------------------------------
With nothing remembered, the same logic suggests nothing specific. This is why run 2's LightGBM suggestion must have come from memory.

[USER] What should I try next?
[MODEL] act  -> retrieve_memory({'query': 'model choice'})
[TOOL OK retrieve_memory] {'documents': [], 'count': 0}
[MODEL] say  -> Memory holds no model preference, and the user gave none this run, so I have nothing specific to suggest yet — I'd start from a plain baseline and ask.

[FINAL] Memory holds no model preference, and the user gave none this run, so I have nothing specific to suggest yet — I'd start from a plain baseline and ask.
